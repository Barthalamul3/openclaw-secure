# Dependencies and Builds
node_modules/
dist/
build/
*.log

# System & Environment
.DS_Store
.env

# SECURITY: CRITICAL IGNORES
# Never commit the runtime config (even though it's in RAM, just in case)
openclaw-runtime.json
# Never commit credentials or sessions
*.gpg
*.cred
gpg_passphrase
bw_session
